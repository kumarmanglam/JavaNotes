```java
import java.util.Stack;

Stack<String> stack = new Stack<String>();

stack.push("apple");          // Push element onto stack
stack.pop();                  // Pop element from stack
stack.peek();                 // Peek at top element without removing

size of stack?
```

 