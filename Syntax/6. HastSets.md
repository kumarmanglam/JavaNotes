
```java
import java.util.HashSet;
HashSet<Character> vowels = new HashSet<>();

char[] vowelChars = {'a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U'};
for (char v : vowelChars) {
	vowels.add(v);
}


HashSet<String> set = new HashSet<String>();

set.add("apple");             // Add element
set.remove("apple");          // Remove element
set.contains("apple");        // Check if set contains element


Convert to Array?
length of hashset?

  
## Set to array
int[] ans = set2.stream().mapToInt(i -> i).toArray();

```

