```java
import java.util.LinkedList;
import java.util.Queue;

Queue<String> queue = new LinkedList<String>();

queue.add("apple");           // Add element to the end
queue.poll();                 // Remove and return element from the front
queue.peek();                 // Peek at the front element without removing

queue.offer();

isEmpty()??
```

